#watchclip and open youtube with mpv in queue
watchclip youtube | while read r
#watchclip send to stack and cycle read from stck (when player close, read next vlue)
do
echo get $r
cclive --exec 'vlc "%f" &'  $(xsel -o -b) # f & for paralel
done

#youtube-watchclip
